"""Repo Intelligence — repository snapshot ingestion and code indexing."""

# Import from submodules directly (e.g. services.repo_intelligence.pipeline)
# to avoid circular imports with db.repository_store.
